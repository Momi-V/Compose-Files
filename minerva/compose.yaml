services:
  minerva:
    build: https://github.com/Momi-V/Compose-Files.git#main:/minerva
    container_name: minerva
    environment:
      - TZ=Europe/Berlin
    restart: unless-stopped
    stdin_open: True
    tty: True
    volumes:
      - /mnt/bigSpin/net/media/minerva:/root/.minerva-dpn
