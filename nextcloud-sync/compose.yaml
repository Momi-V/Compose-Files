services:
  nextcloud-sync:
    build: https://github.com/Momi-V/Compose-Files.git#main:nextcloud-sync
    container_name: nextcloud-sync
    env_file: .env
    environment:
      - LOCALDIR=( "FOLDER/ONE" "FOLDER/TWO" "ETC" )
      - REMOTEDIR=( "FOLDER/ONE" "FOLDER/TWO" "ETC" )
      - USERNAME=USERNAME
      - PASSWORD=PASSWORD
      - SERVER=my.nextcloud.host
      - SLEEP=300
      - TZ=ETC/UTC
    restart: unless-stopped
    volumes:
      - /HOSTPATH:/CONTAINERPATH
